{
  "name": "@founder-os/website",
  "version": "1.0.0",
  "private": true,
  "description": "Founder OS landing page — static, zero-build.",
  "license": "MIT",
  "scripts": {
    "dev": "npx --yes serve public -l 4321",
    "build": "node scripts/build.js",
    "test": "node scripts/check.js",
    "lint": "node scripts/check.js",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\""
  }
}
